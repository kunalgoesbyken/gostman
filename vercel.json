{
    "framework": "vite",
    "buildCommand": "cd gostman-gui/frontend && npm install && npm run build:web",
    "outputDirectory": "gostman-gui/frontend/dist-web",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/$1"
        },
        {
            "source": "/((?!api/|assets/).*)",
            "destination": "/index.html"
        }
    ]
}
